# ==================================
# Project-specific / Environment Files
# ==================================

# Critical environment variables
# Explicitly ignore .env in the backend folder
backend/.env
# Also ignore common local environment files that might appear anywhere
.env.local
.env.development.local
.env.test.local
.env.production.local

# ==================================
# Node.js / React (Frontend and Backend common)
# ==================================
node_modules/      # Ignores all node_modules/ directories, wherever they are (e.g., frontend/node_modules/, backend/node_modules/)
dist/              # Common distribution/output folders
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.parcel-cache/
.cache/
.eslintcache
*.log              # General log files (catch-all for JS/Node logs)

# Create React App specific (if you only build in frontend)
# yarn.lock # Uncomment if you want to ignore yarn.lock
# package-lock.json # Uncomment if you want to ignore package-lock.json (though usually kept for consistent builds)

# Next.js specific (if you use it instead of Create React App)
.next/

# ==================================
# Python (Data Ingestion)
# ==================================
# Python virtual environments (explicitly in backend)
backend/venv/
backend/.venv/ # In case you use .venv instead of venv inside backend

# Compiled Python files
__pycache__/
*.pyc
*.pyo

# Packaging and build artifacts
*.egg-info/
.Python/           # Often created by virtualenv or system Python
pip-log.txt
pip-delete-this-directory.txt
wheel/             # Generated by wheel packaging
*.whl              # Wheel distribution files

# Test and analysis related (based on your previous list)
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/ # Jupyter Notebook checkpoints
testcase/          # If this is a directory for test cases you don't want tracked
authors            # Common file in some Python package structures
typed              # Common file in some Python package structures

# Metadata files that are part of Python package structure, usually generated
METADATA
top_level.txt
*.pyi              # Python stub files (type hints, usually generated/installed)
*.h                # C header files (often part of compiled Python extensions)

# ==================================
# Database Specific (PostgreSQL)
# ==================================

# These entries are for other file-based databases or local temp files.
*.sqlite
*.sqlite3
*.db

# ==================================
# Operating System & IDE specific files
# ==================================
.DS_Store          # macOS
Thumbs.db          # Windows
*.tmp              # General temporary files
*.swp              # Vim swap files
*.swo              # Vim swap files

# IDE specific folders (uncomment if you want to ignore these)
# .vscode/         # VS Code settings for the workspace
